<?xml version="1.0" encoding="utf-8"?>
<!-- 历史录音文件列表页（设计稿 ui_design/screens/recording_file_list.png） -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="72dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="录音文件"
            android:textColor="?attr/recorderColorTextFileTitle"
            android:textSize="18sp"
            android:textStyle="bold" />
    </FrameLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="5dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <!-- clipChildren/clipToPadding=false：卡片外阴影溢出列表边界后延伸到 padding 区 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:paddingStart="18dp"
            android:paddingEnd="18dp"
            android:paddingBottom="20dp">

            <TextView
                android:id="@+id/tvFilesEmpty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="暂无录音文件，连接设备后将自动同步"
                android:textColor="?attr/recorderColorTextSecondary"
                android:textSize="13sp"
                android:visibility="gone" />

            <com.recorder.client.RecordingListView
                android:id="@+id/fileList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
